系统样式 = {
    "环境背景": "#F9F9F7",       # 暖白背景
    "主线框":   "#4A4A4A",       # 深灰
    "球体轮廓": "#D4D4D4",       # 浅灰
    "球体填充": "#F0F4F6",       # 极轻微蓝灰
    "底面填充": "#D5E4E6",       # 马卡龙淡青
    "高亮红色": "#E5989B",       # 莫兰迪柔粉红
    "辅助虚线": "#99AAB5"        # 蓝灰虚线
}

图形系统.初始化状态:
    球半径_R = 1.0
    当前高度_h = 0.577           # 默认初始值为 1/√3 (最大体积点)
    显示辅助面_开关 = 真

图形X.3D空间模型:
    球心O = [0, 0, 0]
    计算底面半径 r = 根号(球半径_R^2 - 当前高度_h^2)

    绘制.球体背景(中心=球心O, 半径=球半径_R, 颜色=球体填充)
    绘制.球心O(点, 颜色=主线框)

    # 动态投影计算底面四个顶点 (绕Z轴旋转45度以获得良好透视)
    顶点V1 = [r * cos(45), r * sin(45), -当前高度_h]
    顶点V2 = [-r * cos(45), r * sin(45), -当前高度_h]
    顶点V3 = [-r * cos(45), -r * sin(45), -当前高度_h]
    顶点V4 = [r * cos(45), -r * sin(45), -当前高度_h]

    绘制.四边形底面(V1, V2, V3, V4, 填充=底面填充, 边框=主线框)
    绘制.侧棱(从=球心O, 到=各顶点, 前方实线, 后方虚线)
    绘制.高(从=球心O, 到=底面中心, 颜色=高亮红色, 粗细=2.0)

图形Y.函数曲线分析:
    自变量横轴 = 高度 h (范围 [0, 1])
    因变量纵轴 = 体积 V(h) = (2/3) * h * (1 - h^2)

    绘制.坐标系(横轴="h", 纵轴="V")
    绘制.理论曲线(样式=实线, 颜色=主线框)
    绘制.极值标记点(坐标=[0.577, 0.256], 颜色=高亮红色)
    绘制.当前状态点(随着滑动条动态移动)

交互.控制面板:
    拖拽.高度滑块(范围=[0.0, 1.0], 默认=0.577):
        接收 变化值:
            当前高度_h = 变化值
            重绘图形()

    点击.设为最大体积按钮():
        当前高度_h = 0.577
        重绘图形()
